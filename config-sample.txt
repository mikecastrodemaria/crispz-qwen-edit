{
    "_hf_token_help": "Hugging Face token for GATED models (e.g. FLUX.1-Krea-dev). Create a READ token at https://huggingface.co/settings/tokens and accept the model license first. Leave empty if you ran 'huggingface-cli login'. config.txt is gitignored -> never commit your token.",
    "hf_token": "",

    "ollama_url": "http://localhost:11434",
    "ollama_describe_prompt": "You are an expert text-to-image prompt writer. Look at the image and output ONE detailed prompt as comma-separated visual tags (subject, clothing, setting, lighting, style, quality). No preamble, no explanation, just the prompt.",
    "ollama_improve_prompt": "Rewrite the following text-to-image prompt to be more vivid and detailed while keeping the same subject and intent. Output ONLY the improved prompt (comma-separated), no preamble.\n\nPROMPT: {prompt}",
    "ollama_compose_prompt": "You are an expert text-to-image prompt writer. Below are descriptions of several reference images. Merge their key elements (subject, clothing, pose, setting, style) into ONE single coherent, detailed image prompt. Output ONLY the prompt (comma-separated), no preamble.\n\n{descriptions}",
    "ollama_vision_blocklist": [],
    "ollama_keep_alive": 0,
    "ollama_cpu": false,
    "job_queue": {"enabled": true},
    "_job_queue_help": "Job queue panel ('+ Queue' snapshots all settings incl. model; 'Run queue' chains them; ⏸ Pause finishes the current job then halts; Stop interrupts it mid-render and the interrupted job STAYS queued). The queue is saved to cache/queue.json after every change and every job, and restored at startup (persist=false to disable). enabled=false removes the UI entirely (zero cost).",
    "tag_autocomplete": {"enabled": true, "max_results": 8, "sources": ["https://raw.githubusercontent.com/DominikDoom/a1111-sd-webui-tagcomplete/main/tags/danbooru.csv"]},
    "_tag_autocomplete_help": "Prompt/negative autocomplete. CSVs in sources are downloaded once to tags/ (any .csv dropped there is also used, rich 'name,cat,count,aliases' or one word per line) + local __wildcards__. enabled=false: no download, no JS injected (zero cost).",
    "xyz_grid": {"enabled": true, "max_jobs": 100, "thumb": 512, "suggest": true},
    "_xyz_grid_help": "X/Y/Z comparison grid (needs job_queue). Each combo becomes a queued job; an annotated contact sheet is assembled per Z value. max_jobs caps the combo count; thumb = cell size in px; suggest = contextual value suggestions (placeholder + fill button) per axis.",
    "load_progress": {"enabled": true, "target_vram_gb": 14.0, "heartbeat_s": 2.0},
    "_load_progress_help": "Live feedback while a model loads (first run downloads from HF = minutes of silence otherwise). Terminal shows a rewritten '[crispz][load] ... 45s | 3.2 GB in VRAM' line every heartbeat_s; the UI progress bar advances (VRAM allocated / target_vram_gb). enabled=false loads directly with no monitor thread (zero cost).",
    "caption_model": "blip-large",
    "_caption_model_help": "Local auto-describe model (no Ollama): blip-large (default) | blip-base",
    "improve_local_keywords": "highly detailed, sharp focus, professional photography, intricate details, natural lighting, high quality, 8k",
    "_improve_local_keywords_help": "Quality tags appended by 'Improve prompt' when no Ollama model is selected (local rule-based fallback, no model).",

    "_performance_presets_help": "name -> [steps, guidance] or [steps, guidance, lora_file]. A 3rd value applies that LoRA (slot 1) when the preset is selected. A distilled Lightning LoRA enables 8 steps + guidance 1 (no CFG) = fast & good. Put the LoRA in loras_dir and set its filename here.",
    "performance_presets": {
            "Lightning (8 steps)": [
                    8,
                    1.0,
                    "PUT_YOUR_LIGHTNING_LORA.safetensors"
            ],
            "Balanced (24 steps)": [
                    24,
                    4.0
            ],
            "Quality (40 steps)": [
                    40,
                    4.0
            ]
    },
    "_model_profiles_help": "Auto steps/guidance when a checkpoint is selected, matched by substring on the file name (first match wins, in order). Keep specific keys (rapid, lightning) BEFORE generic ones (qwen): Lightning/Rapid merges are distilled -> few steps, guidance 1.0 (CFG off).",
    "model_profiles": {
            "rapid": {
                    "steps": 8,
                    "guidance": 1.0
            },
            "lightning": {
                    "steps": 8,
                    "guidance": 1.0
            },
            "qwen": {
                    "steps": 30,
                    "guidance": 4.0
            },
            "edit": {
                    "steps": 30,
                    "guidance": 4.0
            }
    },
    "default_model_profile": {"steps": 30, "guidance": 4.0},

    "_default_loras_help": "LoRAs applied at startup (in loras_dir). Names or [name, weight]. Empty = none. Set a Lightning LoRA here + default_performance 'Lightning (8 steps)' + default_gen_steps 8 + default_guidance 1.0 for a fast default.",
    "default_loras": [],
    "_default_guidance_help": "Qwen 'guidance' = true_cfg_scale (real CFG, enables negative prompt). ~3-5 recommended.",
    "_default_refine_help": "Upscale/refine pass OFF by default (txt2img only = fast startup). NB: the diffusion refine needs a non-quantized base; with a GGUF model it is skipped/limited. Enable per-run via the Upscale checkbox.",
    "_zimage_omni_base_help": "Base edit repo providing text-encoder/VAE/processor when 'zimage_omni_model' is a .gguf transformer. Default Qwen/Qwen-Image-Edit-2509 (downloads ~17GB once).",
    "zimage_omni_base": "Qwen/Qwen-Image-Edit-2509",
    "default_prompt": "",
    "default_negative_prompt": "",
    "default_styles": [],
    "default_aspect_ratio": "1024 x 1024  (1:1)",
    "_force_upscale_ratio_help": "Fooocus-style: force a ratio on Upscale/img2img on the INPUT ('W:H' or 'WxH', e.g. '13:19' or '832x1216'). Empty = keep the input's native ratio. Also toggled per-run by the 'Force aspect ratio' radio (uses the Aspect ratio dropdown).",
    "force_upscale_ratio": "",
    "_force_ratio_mode_help": "How the forced ratio is reached: 'crop' = centre-crop the input to fit (edges are lost), 'extend' = outpaint the missing bands (nothing is lost, the added bands are generated).",
    "force_ratio_mode": "crop",
    "_force_ratio_extend_denoise_help": "Extend mode only: after outpainting the bands, one LIGHT img2img pass over the extended image blends the band seams - only the bands + a feathered transition margin are composited from it, the original centre stays untouched. 0 = off. 0.15-0.30 sensible.",
    "force_ratio_extend_denoise": 0.22,
    "default_performance": "Balanced (24 steps)",
    "default_width": 1024,
    "default_height": 1024,
    "default_gen_steps": 24,
    "default_guidance": 4.0,
    "default_sampler": "euler",
    "default_schedule": "sgm_uniform",
    "default_image_number": 1,
    "default_seed": -1,

    "default_factor": 2.0,
    "default_denoise": 0.30,
    "default_refine": false,
    "default_refine_first": false,
    "default_refine_steps": 12,
    "default_tile": 760,
    "default_overlap": 32,
    "default_refine_tile": 0,
    "default_refine_overlap": 64,
    "default_cpu_offload": "model",
    "attention_slice_above": 1664,
    "auto_refine_tile_above": 1664,
    "refine_tile_denoise_cap": 0.40,
    "refine_tile_prompt": "",

    "checkpoints_dir": "",
    "loras_dir": "",
    "wildcards_dir": "",
    "default_lora_weight": 1.0,
    "_default_lora_weight_help": "Weight a LoRA slot starts at. Must sit inside lora_weight_min..lora_weight_max (it is clamped into the range otherwise).",
    "lora_weight_min": -2.0,
    "lora_weight_max": 2.0,
    "_lora_weight_range_help": "Min/max of the LoRA 'Weight' sliders (Models > LoRA). NEGATIVE weights are allowed and useful: a LoRA applied at a negative weight pushes AWAY from what it was trained on (e.g. a 'skinny slider' LoRA at -1 gives the opposite effect, an 'age slider' at -0.5 makes the subject younger/older the other way). 0 = LoRA has no effect. Typical range: -2..2; beyond ~±1.5 most LoRAs burn/deform the image. Set lora_weight_min to 0 if you never want negatives. If the values are not numbers, or min >= max, the app falls back to -2..2 and logs it.",
    "lora_slots": 3,
    "zimage_omni_model": "Qwen/Qwen-Image-Edit-2509",
    "_hand_detailer_help": "Tick '🖐 Detail hands' by default: after each render, detected hands are re-refined at high resolution like faces are. Needs the optional 'ultralytics' package (requirements-extra.txt); without it the feature logs a message and does nothing.",
    "hand_detailer": false,
    "_hand_detailer_denoise_help": "Strength of the per-hand refine pass (0.1-0.7). Hands usually want more than faces (0.4-0.5) to fix fingers; too high invents a different hand.",
    "hand_detailer_denoise": 0.4,
    "_hand_detailer_max_hands_help": "How many hands to refine per image (largest first).",
    "hand_detailer_max_hands": 4,
    "_hand_detailer_margin_help": "How much context to include around a detected hand (fraction of its size). Tighter than the face margin on purpose: too wide regenerates the forearm and background.",
    "hand_detailer_margin": 0.35,
    "_hand_detailer_model_help": "Hand detection model: a file name from the Bingsu/adetailer HF repo ('hand_yolov8n.pt' = fast default, 'hand_yolov8s.pt' = more accurate) or an absolute path to your own YOLO .pt.",
    "hand_detailer_model": "hand_yolov8n.pt",
    "_hand_detailer_conf_help": "Detection confidence threshold (0-1). Lower catches more hands (and more false positives).",
    "hand_detailer_conf": 0.3,
    "_hand_detailer_device_help": "onnxruntime provider for the hand DETECTOR: 'cpu' (default, CPUExecutionProvider, ~0.1 s per image) or 'cuda' (CUDAExecutionProvider, same stack insightface uses). The detector runs as a pure ONNX session: the torch YOLO model is exported to ONNX once, in a SUBPROCESS, into cache/. This is a hard requirement, not a preference - loading the ultralytics/torch model inside the app process corrupts the shared diffusion weights during offload transfers (proven by checksum 2026-08-17: the text encoder's weight sum drifted on every call once the YOLO model was merely resident in memory, no predict needed; renders degrade to mosaic then NaN).",
    "hand_detailer_device": "cpu",
    "_hand_detailer_prompt_help": "Prompt used to refine each hand CROP. Empty (default) = the img2img pass only sharpens what the source crop already contains - the right behaviour, for the same reason as refine_tile_prompt: feeding the SCENE prompt to a local crop makes the model repaint the subject inside it (observed: a tiny face embedded between thumb and index). Set a short generic string like 'detailed hand, natural fingers' if you want light guidance; never the scene description.",
    "hand_detailer_prompt": "",
    "_face_detailer_prompt_help": "Prompt used to refine each face CROP. Empty (default), same principle as hand_detailer_prompt and refine_tile_prompt: with the SCENE prompt, whatever the prompt describes gets painted onto the face - observed with a prompt featuring a 'CRISPZ STUDIO' sign, the refined face came back with the words written on the cheeks. A short generic string ('detailed face, sharp eyes') is fine; never the scene description.",
    "face_detailer_prompt": "",
    "face_detailer": false,
    "face_detailer_denoise": 0.35,
    "face_detailer_max_faces": 4,
    "faceswap_model_path": "",
    "faceswap_model_url": "",
    "faceswap_restore": true,
    "faceswap_restore_blend": 0.8,
    "faceswap_restore_path": "",
    "faceswap_restore_url": "https://huggingface.co/facefusion/models-3.0.0/resolve/main/gfpgan_1.4.onnx",
    "faceswap_restore_model": "codeformer",
    "_faceswap_restore_model_help": "Face enhancer run after the swap: codeformer (default, recovers more detail from the 128px swap) or gfpgan (lighter). inswapper only outputs 128px, so without a restore pass the face looks soft on any large image. Falls back to gfpgan if CodeFormer cannot be fetched.",
    "faceswap_restore_fidelity": 0.7,
    "_faceswap_restore_fidelity_help": "CodeFormer fidelity weight: 0 = maximum detail (more generative), 1 = maximum fidelity to the swapped pixels. A 128px swap is a heavy degradation, so 0.5-0.7 works best. Ignored by GFPGAN.",
    "faceswap_codeformer_path": "",
    "faceswap_codeformer_url": "https://huggingface.co/facefusion/models-3.0.0/resolve/main/codeformer.onnx",
    "faceswap_occlusion": true,
    "_faceswap_occlusion_help": "Occlusion mask (XSeg). insightface pastes the swapped face through a plain RECTANGLE, so anything in front of the face - a hand, food, a mic, a strand of hair - is painted over by the generated pixels. This mask detects those pixels and keeps the original ones. This is the setting that fixes broken mouths on 'person eating/drinking' shots. Needs dfl_xseg.onnx (auto-downloaded); disabled cleanly if unavailable.",
    "faceswap_occluder_path": "",
    "faceswap_occluder_url": "https://huggingface.co/facefusion/models-3.0.0/resolve/main/dfl_xseg.onnx",
    "faceswap_regions": true,
    "_faceswap_regions_help": "Face-region mask (BiSeNet / CelebAMask-HQ): restricts the swap to skin, brows, eyes, glasses, nose, mouth and lips, so it does not bleed onto hair, neck, clothes or background. Needs bisenet_resnet_34.onnx (auto-downloaded); disabled cleanly if unavailable.",
    "faceswap_parser_path": "",
    "faceswap_parser_url": "https://huggingface.co/facefusion/models-3.0.0/resolve/main/bisenet_resnet_34.onnx",
    "faceswap_color_match": true,
    "_faceswap_color_match_help": "Match the swapped face's colour statistics (LAB mean/std) to the original face, so the source photo's skin tone and exposure do not show through.",

    "default_save_mode": "local",
    "default_output_dir": "out",
    "default_output_format": "png",
    "filename_pattern": "{date}_{tag}_seed{seed}_{w}x{h}{index}",
    "date_subfolders": true,
    "metadata_scheme": "crispz",
    "wildcards_in_order": false,
    "save_pre_upscale": false,
    "gguf_arch": "qwen_image",
    "_gguf_arch_help": "Architecture accepted for .gguf checkpoints, read from the file header (general.architecture). Only matching files are listed in the checkpoint dropdown; the rest are skipped at startup with a reason, instead of failing after a long load. Known values: \"flux\" (FLUX.1 dev/schnell/Krea), \"qwen_image\", \"krea2\" (Krea 2 -- its OWN non-Flux architecture, needs ComfyUI + its own encoder/VAE, not loadable here), plus llama/gemma3/... for LLM GGUFs. A GGUF whose header cannot be read is kept (never discard a valid model on a doubt).",
    "civitai_api_key": "",
    "auth": "",
    "_auth_help": "Login page for the UI. Empty = no login (fine on localhost). Set to \"user:password\" (several accounts: \"a:pw1,b:pw2\") BEFORE exposing crispz-studio on your LAN or through a tunnel (cloudflared etc.) - otherwise anyone with the URL can generate images and browse/delete your outputs. Also settable via --auth or the CRISPZ_AUTH env var.",
    "civitai_batch": {"enabled": true, "sleep": 0.5, "check_updates": true},
    "_civitai_batch_help": "Batch CivitAI enrichment. enabled=true shows the '🔄 Fetch all missing' button on the Asset Browser LoRAs/Models tabs and lets cz_civitai_batch.py (civitai_index.bat/.sh) run. sleep = seconds between requests (rate-limit friendly); check_updates = also flag models that have a newer version on CivitAI. enabled=false hides the button (per-model 🔎 fetch still works).",
    "asset_browser": {
        "enabled": false,
        "generate_thumbnails": true,
        "thumbnail_size": 256,
        "thumbnail_quality": 85,
        "thumb_workers": 0,
        "_thumb_workers_help": "Threads used to generate thumbnails (0 = auto: min(8, CPU cores)). Thumbnails are made in parallel, both for background indexing and for the Asset Browser's 'Rebuild ALL thumbnails (force)' button. Lower it if indexing a huge folder makes the machine sluggish.",
        "blur_thumbnails": false,
        "cache_dir": "",
        "_cache_dir_help": "Where thumbnails are cached. Empty (default) = <app>/cache (the app folder, gitignored - usually a fast disk, much faster than an HDD/NAS output folder). 'output' = next to the images (<out>/_index/thumbs, relative URLs). Any path = custom cache (e.g. an SSD): thumbnails go to <cache_dir>/crispz-thumbs/<slug>/. Also settable in the UI (Save > Asset Browser). Safe to delete at any time (rebuilt on demand)."
    },

    "provenance_watermark": "off",
    "_provenance_watermark_help": "on = embed an invisible TrustMark watermark (Adobe, open source) in every saved image, for machine-readable AI disclosure (EU AI Act art. 50, applicable Aug 2 2026). Needs 'pip install trustmark' (see requirements-extra.txt); model (~40 MB) downloads on first use, then ~0.1s/image on CPU (the GPU is never touched). off (default) = no watermark. Read watermarks anytime via PNG Info > Check invisible watermark, or cz.bat --provenance -i img.png.",
    "provenance_wm_id": "crispzAI",
    "_provenance_wm_id_help": "Identifier embedded in the TrustMark watermark. Max 9 ASCII characters (error-corrected payload is ~68 bits) - longer values are truncated. E.g. your studio tag.",

    "dequant_cache": "auto",
    "_dequant_cache_help": "Where the bf16 result of a FP8/INT8 dequant is cached, so the SAME checkpoint loads in seconds next time instead of minutes. 'auto' (default) = <app>/cache/dequant (gitignored), any path = custom folder, 'off' = never cache. Each entry weighs as much as the BF16 build; safe to delete any time (rebuilt on demand).",
    "dequant_cache_max_gb": 60,
    "_dequant_cache_max_gb_help": "Size cap of that cache in GB; least-recently-used entries are evicted past it. 0 = unlimited.",
    "log_level": "info"
}
